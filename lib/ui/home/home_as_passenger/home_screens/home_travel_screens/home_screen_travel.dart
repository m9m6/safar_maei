import 'package:flutter/material.dart';

class HomeScreenTravel extends StatelessWidget {
  const HomeScreenTravel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
