import 'package:flutter/material.dart';

class HomeScreenTransfer extends StatelessWidget {
  const HomeScreenTransfer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
