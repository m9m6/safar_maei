import 'package:flutter/material.dart';

class OnAWay extends StatelessWidget {
  const OnAWay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
