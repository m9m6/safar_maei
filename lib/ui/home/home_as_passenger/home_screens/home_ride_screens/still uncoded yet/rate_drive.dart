import 'package:flutter/material.dart';

class RateDrive extends StatelessWidget {
  const RateDrive({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
